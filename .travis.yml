language: cpp

matrix:
  include:
    - os: linux
      addons: { apt: {
        packages: [g++-6, libboost],
        sources: [ubuntu-toolchain-r-test, sourceline: 'ppa:mhier/libboost-latest'] } }
      env: MATRIX_EVAL="CC=gcc-6 && CXX=g++-6" BUILD_TYPE=Release

before_install:
  - eval "${MATRIX_EVAL}"

script:
script:
  - mkdir build && cd build
  - cmake -DCMAKE_BUILD_TYPE=$BUILD_TYPE ..
  - cmake --build . -- VERBOSE=1 --no-print-directory -j2
  - ctest -VV .

notifications:
  email: false
