#ifndef HASM_HASMCONFIG_HPP
#define HASM_HASMCONFIG_HPP

namespace Hasm {
namespace Config {

const int VERSION_MAJOR{${hasm_VERSION_MAJOR}};
const int VERSION_MINOR{${hasm_VERSION_MINOR}};
const int VERSION_PATCH{${hasm_VERSION_PATCH}};

} // namespace Config
} // namespace Hasm

#endif
